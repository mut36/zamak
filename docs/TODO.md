# TODO

나중에 다시 볼 일 있는 후속 작업들. 완료하면 지우지 말고 취소선 처리(이유 남기기).

## enrich

### 이용자 증가 시: era/tone 추출을 그라운딩 → 모델 자체 지식으로 전환 검토 (2026-07-24)

`app/lib/server/enrichMovie.ts`의 `extractKeywords`(TMDB가 작품을 찾은 경우 era/tone을
뽑는 호출)는 현재 **구글 그라운딩**(`googleSearch` 툴)을 쓴다.

**왜 그라운딩인가**: 그라운딩 없이 모델 자체 지식만 쓰면, 마이너한 작품에서 모델이
실제 극중 시대를 몰라 우리가 넘겨준 **개봉·제작 연도**를 그대로 "배경/시대"로 답해버리는
버그가 있었다(2022년작·1978년 배경물에서 재현). 그라운딩은 모델이 실제 줄거리를
검색해서 답하게 해 이 문제를 근본적으로 해결한다.

**왜 나중에 재검토할 가치가 있는가**: 그라운딩 호출은 검색 없는 호출보다 비용·지연이
더 든다. 지금은 파일당 1회 호출이라 번역 비용 전체에 비하면 미미하지만, **이용자
수가 늘어 이 비용이 눈에 띄기 시작하면** 아래 순서로 재검토:

1. 그라운딩 호출의 실제 단가·지연을 실측 (`docs/tuning/`에 있는 실측 방법론 참고)
2. 모델 지식만으로 되돌리되, 버그를 재발시키지 않을 방법이 있는지 검토:
   - TMDB `overview`(줄거리)를 이 프롬프트에만 참고 자료로 주입(최종 번역 프롬프트에는
     여전히 안 보냄 — 필요 없다고 확정됨, 2026-07-24)해서 모델이 실제 배경을 추론할
     근거를 주는 절충안
   - 또는 인지도 높은 작품(예: TMDB popularity 상위)만 모델 지식으로 처리하고,
     그 외에는 계속 그라운딩하는 하이브리드
3. 어느 쪽이든 **회귀 테스트**(`app/lib/server/enrichMovie.test.ts`의 "extracts era/tone
   via grounded search..." 테스트)가 그대로 통과하는지 반드시 확인 — 이 테스트가 정확히
   이 버그의 재발을 잡기 위해 만들어졌다.

## 번역 품질 (2026-07-24 논의)

- [x] ~~**인지 경계(장면 갭) 청킹**~~ — 완료 (2026-07-25, 커밋 `54bbc71`).
  `app/lib/srt.ts` `chunkSrtBlocksAtGaps`가 목표 크기 ±20% 창에서 가장 큰 2초+ 갭에
  맞춰 자름. 실측상 컷이 5~9초급 장면 전환에 떨어져 비용 0으로 경계 문제를 크게 완화.
- [ ] **청크 경계 겹침(overlap) 컨텍스트** — *조건부 보류.* 컷 **위치**는 위 gap 청킹으로
  해결됐으므로, 이건 "그래도 대화 중간이 잘려 문맥이 깨지는 잔여 사례가 측정되면" 그때
  추가한다. overlap은 참고 블록만큼 토큰 비용이 늘고 출력 블록 수 계약(입력=출력)을
  건드려 재번호 밀림 위험이 있어, 근거(잔여 문제 측정) 나오기 전엔 안 넣는다.
- [ ] **고유명사·호칭·용어 글로사리** — 인물명/지명/조직명의 한국어 표기를 파일 전체에서
  고정. 청크마다 병렬 번역되다 보니 같은 이름이 청크별로 다르게 음역되는 문제 해결.
  (gap 청킹 완료 후 현재 최우선 후보.)
- [ ] **인물별 말투 지침(캐릭터 시트)** — 명시적으로 보류 중. 자막 화자·청자 추론은
  모델 자체의 추론 능력이 핵심이라, 정적 인물 시트가 오히려 관계가 변하는 장면(초반
  존댓말→후반 반말 등)의 아크를 얼려버릴 위험이 있다는 게 2026-07-24 논의 결론.
  다시 검토한다면 TMDB cast(출연진)를 이름 앵커로 활용하는 방안부터.

## 고급 번역 (길이 예산)

- [ ] **CPS 기반 길이 예산** — 자막의 화면 체류 시간에 맞춰 번역문 길이를 조절(1.2초짜리
  줄은 짧게, 4초짜리는 여유). 계산 프리미티브 `computeCps`(`app/lib/srt.ts`)는 이미
  준비됨(2026-07-25, 커밋 `7dbb660`) — 아직 어떤 기능에도 미연결. 실제 기능(프롬프트에
  블록별 길이 힌트 주입 등)은 미착수.

## 에러 폴백 (2026-07-26, §9.6 도입 시 스코프 아웃)

에러 분류·재시도 예산·전역 중단·부분 실패 보고(§9.6, `translationErrors.ts`/
`chunkRetry.ts`)는 이번에 들어갔지만, 아래 둘은 크레딧 정책이 먼저 정해져야 해서
다음 커밋으로 미뤘다:

- [ ] **크레딧 환불 정책** — 지금은 청크/블록이 얼마나 실패했든 크레딧 1개가 그대로
  소진된다. 후보: (a) 그대로 둔다 (b) 실패율(`failedChunks`+`fallbackBlocks` 기준)이
  일정 임계 초과 시 자동 환불. (b)는 DB 트랜잭션(`translation_jobs` 테이블)이 얽히므로
  별도 커밋으로 뺀다.
- [ ] **"실패한 부분만 재번역" 버튼** — `TranslationResult.failedChunks`/
  `fallbackBlocks`가 이미 어떤 부분이 원문으로 남았는지 알고 있으니, 완료 화면에서
  그 부분만 다시 청크 요청을 보내는 기능. 크레딧 정책(위 항목)이 먼저 정해져야
  "재번역도 크레딧을 또 쓰는가"를 결정할 수 있어 보류.

## AI가 번역 규칙을 가끔 무시하는 문제 (2026-07-26, `enforceTextRules` 도입 시 스코프 아웃)

기계적으로 정답이 하나뿐인 규칙(말줄임표·마침표/쉼표·2줄 상한)은 `srt.ts`
`enforceTextRules`로 코드 강제가 들어갔다(`decisions.md` §2-8). 아래는 검토했지만
이번엔 넣지 않은 것들:

- [ ] **규칙 11 (스타일 태그 위치·의미 유지) 코드 강제** — 원본 블록에 줄 전체를 감싸는
  태그(`<i>...</i>`)나 맨 앞 위치 태그(`{\an8}`)가 있는데 번역 결과에서 빠졌으면
  원본 기준으로 복원. 7·9번과 달리 **원본과 번역 결과를 함께 받아 비교**해야 하는
  다른 모양의 함수라(재조립처럼) 이번 스코프에서 뺐다. 실사용에서 태그 유실이 실제로
  자주 관측되면 우선순위 올릴 것.
- [ ] **규칙 8(25자 줄바꿈)·규칙 6(고어체 금지) 위반율 측정** — 둘 다 의미 판단이 필요해
  자동 고침 대상은 아니지만, "가끔 무시한다"는 지금 감(gut feeling)이지 숫자가 아니다.
  `scripts/prompt-ab.mts`에 정규식 기반 린터(줄 길이, 고어체 어미 패턴)를 추가해
  위반율을 하네스 요약에 찍으면, 다음 프롬프트 수정을 이 프로젝트의 다른 결정들처럼
  실측 기반으로 판단할 수 있다.
- [ ] **`enforceTextRules`의 `TextRuleReport` 집계·표시** — 지금은 콘솔 로그만 남긴다.
  §9.6(에러 분류)처럼 하네스 요약 컬럼이나 화면 표시로 확장하는 건 후속 과제.
