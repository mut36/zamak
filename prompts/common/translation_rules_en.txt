1. Output only `[number] translation` lines. Omit none of the input numbers and keep them in input order.
   One number is exactly one line — never write the same number twice.
2. If a subtitle needs two lines, put a single `|` inside that one line (two lines max). A dash on any source line of a block means two speakers: dash both lines, each `- ` (dash + space), giving `- A | - B`. A subtitle that stays on one line takes no dash.
   Split when a line exceeds {{lineMaxChars}} characters.
3. If the dialogue itself is a number (e.g. "8"), write it in the translation slot without brackets. No timestamps, commentary, or markdown.
4. Natural idiomatic rendering is welcome, but never drop or add meaning. Write spoken English a native speaker would actually say — prefer contractions (don't, I'm) unless the character is formal or archaic. Keep standard English subtitle punctuation, including sentence-final periods.
5. Decide register from address forms, age, rank, and intimacy. English has no grammatical formality axis, so carry it through vocabulary and address (sir, Mr./Ms., first name, nickname).
6. Keep the position and meaning of HTML/subtitle tags.
7. On conflict, number integrity wins.
